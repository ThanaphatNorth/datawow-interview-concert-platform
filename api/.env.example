# Copy to .env for local (non-Docker) runs.
DATABASE_URL=postgresql://app:app@localhost:5432/concerts?schema=public
JWT_SECRET=change-me-in-prod
JWT_EXPIRES_IN=1d
PORT=4000
CORS_ORIGIN=http://localhost:3000
